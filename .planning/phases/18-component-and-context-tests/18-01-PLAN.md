---
phase: 18-component-and-context-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/__tests__/PrimaryButton.test.tsx
  - src/components/__tests__/MealTypePills.test.tsx
  - src/components/__tests__/QuickAddButtons.test.tsx
  - src/components/__tests__/StreakAverageRow.test.tsx
  - src/components/__tests__/GhostReference.test.tsx
  - src/components/__tests__/RestTimerBanner.test.tsx
  - src/components/__tests__/ProgramTargetReference.test.tsx
  - src/components/__tests__/ExerciseCategoryTabs.test.tsx
  - src/components/__tests__/SetListItem.test.tsx
  - src/components/__tests__/MealListItem.test.tsx
autonomous: true
requirements: [COMP-01]

must_haves:
  truths:
    - "PrimaryButton renders its title, invokes onPress, and shows spinner when loading"
    - "MealTypePills renders all 4 meal types and invokes onSelect with the tapped type"
    - "QuickAddButtons renders meal pills and invokes onQuickAdd; returns null when meals is empty"
    - "StreakAverageRow shows streak and average text; returns null when both are zero/null"
    - "GhostReference renders set data in horizontal layout (1-3 sets) and vertical layout (4+ sets); returns null for empty sets"
    - "RestTimerBanner shows countdown and invokes onStop when Skip is pressed"
    - "ProgramTargetReference displays target sets x reps and optional weight"
    - "ExerciseCategoryTabs renders all 7 categories and invokes onSelect with the tapped category"
    - "SetListItem renders set text in reps and timed mode and invokes onDelete"
    - "MealListItem renders meal type, description, protein grams, and invokes onEdit and onDelete"
  artifacts:
    - path: "src/components/__tests__/PrimaryButton.test.tsx"
      provides: "PrimaryButton render and interaction tests"
    - path: "src/components/__tests__/MealTypePills.test.tsx"
      provides: "MealTypePills render and selection tests"
    - path: "src/components/__tests__/QuickAddButtons.test.tsx"
      provides: "QuickAddButtons render, empty state, and callback tests"
    - path: "src/components/__tests__/StreakAverageRow.test.tsx"
      provides: "StreakAverageRow render and null-return tests"
    - path: "src/components/__tests__/GhostReference.test.tsx"
      provides: "GhostReference horizontal, vertical, and empty tests"
    - path: "src/components/__tests__/RestTimerBanner.test.tsx"
      provides: "RestTimerBanner countdown display and skip callback tests"
    - path: "src/components/__tests__/ProgramTargetReference.test.tsx"
      provides: "ProgramTargetReference display tests"
    - path: "src/components/__tests__/ExerciseCategoryTabs.test.tsx"
      provides: "ExerciseCategoryTabs render and selection tests"
    - path: "src/components/__tests__/SetListItem.test.tsx"
      provides: "SetListItem reps/timed display and delete tests"
    - path: "src/components/__tests__/MealListItem.test.tsx"
      provides: "MealListItem render and callback tests"
  key_links:
    - from: "src/components/__tests__/PrimaryButton.test.tsx"
      to: "src/components/PrimaryButton.tsx"
      via: "import { PrimaryButton }"
      pattern: "render.*PrimaryButton"
    - from: "src/components/__tests__/MealTypePills.test.tsx"
      to: "src/components/MealTypePills.tsx"
      via: "import { MealTypePills }"
      pattern: "fireEvent\\.press.*onSelect"
---

<objective>
Write tests for all 10 simple presentational components, verifying they render correctly and invoke callbacks on interaction.

Purpose: Covers COMP-01 requirement -- simple components have rendering and interaction tests.
Output: 10 test files in src/components/__tests__/, all passing via `npx jest src/components/__tests__`.
</objective>

<execution_context>
@.planning/phases/15-test-infrastructure/15-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- All 10 components are pure presentational -- no DB or context imports. -->
<!-- Use @testing-library/react-native render() + fireEvent directly. No providers needed. -->

From src/types/index.ts:
```typescript
export type MealType = 'breakfast' | 'lunch' | 'dinner' | 'snack';
export const MEAL_TYPES: MealType[] = ['breakfast', 'lunch', 'dinner', 'snack'];
export type ExerciseCategory = 'chest' | 'back' | 'legs' | 'shoulders' | 'arms' | 'core' | 'conditioning';
export const EXERCISE_CATEGORIES: ExerciseCategory[] = ['chest', 'back', 'legs', 'shoulders', 'arms', 'core', 'conditioning'];
export interface WorkoutSet {
  id: number; sessionId: number; exerciseId: number; setNumber: number;
  weightKg: number; reps: number; loggedAt: string; isWarmup: boolean;
}
export interface Meal {
  id: number; proteinGrams: number; description: string; mealType: MealType;
  loggedAt: string; localDate: string; createdAt: string;
}
export interface ProgramDayExercise {
  id: number; programDayId: number; exerciseId: number;
  targetSets: number; targetReps: number; targetWeightKg: number;
  sortOrder: number; supersetGroupId: number | null;
}
```

Test pattern for component tests (established Phase 15):
- import { render, fireEvent } from '@testing-library/react-native'
- No renderWithProviders needed for pure components (no context/navigation deps)
- Use getByText, queryByText, getByTestId, getAllByText for assertions
- Use fireEvent.press() for touch handlers
- Use fireEvent.changeText() for TextInput changes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests for PrimaryButton, MealTypePills, QuickAddButtons, StreakAverageRow, and GhostReference</name>
  <files>
    src/components/__tests__/PrimaryButton.test.tsx
    src/components/__tests__/MealTypePills.test.tsx
    src/components/__tests__/QuickAddButtons.test.tsx
    src/components/__tests__/StreakAverageRow.test.tsx
    src/components/__tests__/GhostReference.test.tsx
  </files>
  <read_first>
    src/components/PrimaryButton.tsx
    src/components/MealTypePills.tsx
    src/components/QuickAddButtons.tsx
    src/components/StreakAverageRow.tsx
    src/components/GhostReference.tsx
    src/types/index.ts
  </read_first>
  <action>
Create 5 test files in src/components/__tests__/. Each file uses `import { render, fireEvent } from '@testing-library/react-native'` and imports the component directly. No providers or mocks needed -- these are pure presentational.

**PrimaryButton.test.tsx** -- 4 tests:
1. `renders title text`: render `<PrimaryButton title="Log Set" onPress={jest.fn()} />`, assert `getByText('Log Set')` exists
2. `calls onPress when pressed`: render with `onPress={mockFn}`, `fireEvent.press(getByText('Log Set'))`, assert `mockFn` called once
3. `does not call onPress when disabled`: render with `disabled={true}`, fireEvent.press, assert mockFn NOT called
4. `shows ActivityIndicator when loading`: render with `loading={true}`, assert `queryByText('Log Set')` is null (spinner replaces text)

**MealTypePills.test.tsx** -- 3 tests:
1. `renders all 4 meal type pills`: render `<MealTypePills selected={null} onSelect={jest.fn()} />`, assert `getByText('Breakfast')`, `getByText('Lunch')`, `getByText('Dinner')`, `getByText('Snack')` all exist
2. `calls onSelect with tapped type`: render with `onSelect={mockFn}`, `fireEvent.press(getByText('Lunch'))`, assert `mockFn` called with `'lunch'`
3. `highlights selected pill`: render with `selected="dinner"`, assert the Dinner pill has the selected style (test by checking the pill's parent TouchableOpacity style includes the accent background -- use toJSON() or check style prop)

**QuickAddButtons.test.tsx** -- 3 tests:
1. `returns null when meals is empty`: render with `meals={[]}`, assert `queryByText('RECENT TEMPLATES')` is null
2. `renders meal pills`: render with `meals=[{description: 'Chicken', proteinGrams: 30, mealType: 'lunch'}, {description: 'Shake', proteinGrams: 50, mealType: 'snack'}]`, assert `getByText('Chicken 30g')` and `getByText('Shake 50g')` exist
3. `calls onQuickAdd with tapped meal`: render with `onQuickAdd={mockFn}`, press 'Chicken 30g', assert `mockFn` called with `{description: 'Chicken', proteinGrams: 30, mealType: 'lunch'}`

**StreakAverageRow.test.tsx** -- 4 tests:
1. `returns null when streak=0 and average=null`: render, assert `toJSON()` is null
2. `shows streak when streak > 0`: render with `streak={5} average={null}`, assert text contains '5 day streak'
3. `shows average when average is not null`: render with `streak={0} average={150}`, assert text contains '7-day avg: 150g'
4. `shows both with separator`: render with `streak={3} average={180}`, assert both '3 day streak' and '7-day avg: 180g' present

**GhostReference.test.tsx** -- 4 tests:
1. `returns null for empty sets`: render with `sets={[]}`, assert `toJSON()` is null
2. `returns null for undefined sets`: render with `sets={undefined as any}`, assert `toJSON()` is null
3. `renders horizontal layout for 1-3 sets`: render with 2 WorkoutSet objects (`{id:1, sessionId:1, exerciseId:1, setNumber:1, weightKg:135, reps:10, loggedAt:'', isWarmup:false}` etc), assert `getByText(/Set 1: 135lb/)` exists
4. `renders timed format when isTimed=true`: render with `isTimed={true}` and a set with reps=90 (seconds), assert text contains 'Set 1: 01:30'
  </action>
  <verify>
    <automated>cd C:/Users/eolson/WorkPC-Development && npx jest src/components/__tests__/PrimaryButton.test.tsx src/components/__tests__/MealTypePills.test.tsx src/components/__tests__/QuickAddButtons.test.tsx src/components/__tests__/StreakAverageRow.test.tsx src/components/__tests__/GhostReference.test.tsx --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - PrimaryButton.test.tsx contains `describe('PrimaryButton'` and at least 4 `it(` or `test(` blocks
    - MealTypePills.test.tsx contains `getByText('Breakfast')` and `getByText('Lunch')` and `getByText('Dinner')` and `getByText('Snack')`
    - QuickAddButtons.test.tsx contains `queryByText('RECENT TEMPLATES')` and tests null return for empty meals
    - StreakAverageRow.test.tsx contains `toJSON()` null check and `getByText(/day streak/)`
    - GhostReference.test.tsx contains `getByText(/Set 1/)` and tests isTimed format
    - All 5 test files pass with exit code 0
  </acceptance_criteria>
  <done>5 simple component test files pass: PrimaryButton (4 tests), MealTypePills (3 tests), QuickAddButtons (3 tests), StreakAverageRow (4 tests), GhostReference (4 tests)</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for RestTimerBanner, ProgramTargetReference, ExerciseCategoryTabs, SetListItem, and MealListItem</name>
  <files>
    src/components/__tests__/RestTimerBanner.test.tsx
    src/components/__tests__/ProgramTargetReference.test.tsx
    src/components/__tests__/ExerciseCategoryTabs.test.tsx
    src/components/__tests__/SetListItem.test.tsx
    src/components/__tests__/MealListItem.test.tsx
  </files>
  <read_first>
    src/components/RestTimerBanner.tsx
    src/components/ProgramTargetReference.tsx
    src/components/ExerciseCategoryTabs.tsx
    src/components/SetListItem.tsx
    src/components/MealListItem.tsx
    src/types/index.ts
  </read_first>
  <action>
Create 5 test files in src/components/__tests__/. Same pattern: `import { render, fireEvent } from '@testing-library/react-native'`, direct component import, no providers.

**RestTimerBanner.test.tsx** -- 3 tests:
1. `displays formatted countdown`: render `<RestTimerBanner remainingSeconds={75} totalSeconds={90} onStop={jest.fn()} />`, assert `getByText('1:15')` exists (75 seconds = 1:15)
2. `displays Rest label`: assert `getByText('Rest')` exists
3. `calls onStop when Skip pressed`: render with `onStop={mockFn}`, `fireEvent.press(getByText('Skip'))`, assert `mockFn` called once

**ProgramTargetReference.test.tsx** -- 2 tests:
1. `displays target without weight when weight is 0`: render `<ProgramTargetReference targetSets={3} targetReps={10} targetWeightKg={0} />`, assert `getByText('3x10')` exists, assert `queryByText(/@/)` is null
2. `displays target with weight`: render with `targetSets={4} targetReps={8} targetWeightKg={185}`, assert `getByText('4x8 @ 185lb')` exists

**ExerciseCategoryTabs.test.tsx** -- 3 tests:
1. `renders all 7 categories`: render `<ExerciseCategoryTabs selected={null} onSelect={jest.fn()} />`, assert `getByText('Chest')`, `getByText('Back')`, `getByText('Legs')`, `getByText('Shoulders')`, `getByText('Arms')`, `getByText('Core')`, `getByText('Conditioning')` all exist
2. `calls onSelect with tapped category`: render with `onSelect={mockFn}`, `fireEvent.press(getByText('Back'))`, assert `mockFn` called with `'back'`
3. `highlights selected tab`: render with `selected="legs"` -- verify the Legs pill gets the selected style (check via testID or style inspection)

**SetListItem.test.tsx** -- 3 tests:
1. `renders reps format`: render `<SetListItem set={{id:1, sessionId:1, exerciseId:1, setNumber:2, weightKg:135, reps:10, loggedAt:'', isWarmup:false}} onDelete={jest.fn()} />`, assert `getByText(/Set 2: 135lb .* 10 reps/)` exists
2. `renders timed format`: render with `isTimed={true}` and `reps=90`, assert `getByText(/Set 1: 01:30/)` exists
3. `renders warmup label`: render with `isWarmup: true`, assert text contains '(warmup)'

**MealListItem.test.tsx** -- 3 tests:
1. `renders meal type and protein grams`: render `<MealListItem meal={{id:1, proteinGrams:40, description:'Chicken', mealType:'lunch', loggedAt:'', localDate:'', createdAt:''}} onEdit={jest.fn()} onDelete={jest.fn()} />`, assert `getByText('Lunch:')` and `getByText('40g')` and `getByText('Chicken')` exist
2. `calls onEdit when row tapped`: render, `fireEvent.press(getByText('Chicken'))`, assert `onEdit` called with the meal object
3. `calls onDelete when delete area tapped`: render, `fireEvent.press(getByText('Delete'))`, assert `onDelete` called with the meal object
  </action>
  <verify>
    <automated>cd C:/Users/eolson/WorkPC-Development && npx jest src/components/__tests__/RestTimerBanner.test.tsx src/components/__tests__/ProgramTargetReference.test.tsx src/components/__tests__/ExerciseCategoryTabs.test.tsx src/components/__tests__/SetListItem.test.tsx src/components/__tests__/MealListItem.test.tsx --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - RestTimerBanner.test.tsx contains `getByText('1:15')` and `getByText('Skip')` and fireEvent.press on Skip
    - ProgramTargetReference.test.tsx contains `getByText('3x10')` and `getByText('4x8 @ 185lb')`
    - ExerciseCategoryTabs.test.tsx contains `getByText('Chest')` and `getByText('Conditioning')` and tests all 7 categories
    - SetListItem.test.tsx contains `getByText(/Set 2: 135lb/)` and tests isTimed and warmup
    - MealListItem.test.tsx contains `getByText('Lunch:')` and `getByText('40g')` and tests onEdit and onDelete callbacks
    - All 5 test files pass with exit code 0
  </acceptance_criteria>
  <done>5 more simple component test files pass: RestTimerBanner (3 tests), ProgramTargetReference (2 tests), ExerciseCategoryTabs (3 tests), SetListItem (3 tests), MealListItem (3 tests)</done>
</task>

</tasks>

<verification>
Run the full simple component test suite:
```bash
npx jest src/components/__tests__ --no-coverage
```
All 10 test files should pass. Expected: ~32 tests across 10 suites.
</verification>

<success_criteria>
- All 10 simple component test files exist in src/components/__tests__/
- Each component has at least 2 tests covering rendering and interaction
- All tests pass when run together via `npx jest src/components/__tests__ --no-coverage`
- No tests import from db or context modules (these are pure presentational tests)
</success_criteria>

<output>
After completion, create `.planning/phases/18-component-and-context-tests/18-01-SUMMARY.md`
</output>
