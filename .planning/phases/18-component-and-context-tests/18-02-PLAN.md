---
phase: 18-component-and-context-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/__tests__/RenameModal.test.tsx
  - src/components/__tests__/EditTargetsModal.test.tsx
  - src/components/__tests__/GoalSetupForm.test.tsx
  - src/components/__tests__/ProteinProgressBar.test.tsx
  - src/components/__tests__/PRToast.test.tsx
  - src/components/__tests__/ProteinChart.test.tsx
autonomous: true
requirements: [COMP-02]

must_haves:
  truths:
    - "RenameModal validates empty input, disables Save for unchanged name, calls onSave with trimmed name, and calls onClose on Cancel"
    - "EditTargetsModal validates sets/reps/weight fields and shows error messages for invalid input"
    - "GoalSetupForm validates goal > 0, calls setProteinGoal and onGoalSet on submit"
    - "ProteinProgressBar displays percentage, current/goal text, and toggles to edit mode"
    - "PRToast shows exercise name and PR details when showPR is called via ref"
    - "ProteinChart renders empty state and time range filter pills"
  artifacts:
    - path: "src/components/__tests__/RenameModal.test.tsx"
      provides: "RenameModal validation and submit tests"
    - path: "src/components/__tests__/EditTargetsModal.test.tsx"
      provides: "EditTargetsModal validation tests"
    - path: "src/components/__tests__/GoalSetupForm.test.tsx"
      provides: "GoalSetupForm submit and error tests"
    - path: "src/components/__tests__/ProteinProgressBar.test.tsx"
      provides: "ProteinProgressBar display and edit mode tests"
    - path: "src/components/__tests__/PRToast.test.tsx"
      provides: "PRToast ref-based showPR and render tests"
    - path: "src/components/__tests__/ProteinChart.test.tsx"
      provides: "ProteinChart empty state and filter tests"
  key_links:
    - from: "src/components/__tests__/GoalSetupForm.test.tsx"
      to: "src/db/protein.ts"
      via: "jest.mock('../db') intercepts setProteinGoal"
      pattern: "jest\\.mock.*\\.\\./db"
    - from: "src/components/__tests__/ProteinChart.test.tsx"
      to: "src/db/protein.ts"
      via: "jest.mock('../db') intercepts getDailyProteinTotals"
      pattern: "jest\\.mock.*\\.\\./db"
---

<objective>
Write tests for the 6 complex components and modals, covering validation logic, empty states, form submission, and ref-based APIs.

Purpose: Covers COMP-02 requirement -- complex components and modals have tests including validation.
Output: 6 test files in src/components/__tests__/, all passing via `npx jest`.
</objective>

<execution_context>
@.planning/phases/15-test-infrastructure/15-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Complex components that need DB mocking or special patterns -->

GoalSetupForm imports: `import { setProteinGoal } from '../db'`
  - Must jest.mock('../db', () => ({ setProteinGoal: jest.fn() }))

ProteinProgressBar imports: `import { setProteinGoal } from '../db'`
  - Must jest.mock('../db', () => ({ setProteinGoal: jest.fn() }))

ProteinChart imports: `import { getDailyProteinTotals } from '../db'`
  and `import { useFocusEffect } from '@react-navigation/native'`
  - Must jest.mock('../db', () => ({ getDailyProteinTotals: jest.fn() }))
  - Needs NavigationContainer wrapper OR mock useFocusEffect to call callback immediately

PRToast: Uses forwardRef + useImperativeHandle. Access showPR via React.createRef<PRToastHandle>().
  - Uses useSafeAreaInsets (already mocked via __mocks__/react-native-safe-area-context.js)
  - Uses Animated.timing (runs in test environment)

RenameModal, EditTargetsModal: Pure React Native Modal + TextInput. No DB imports.
  - Must pass visible={true} to render modal content

From src/components/PRToast.tsx:
```typescript
export interface PRToastHandle {
  showPR: (exerciseName: string, reps: number, weightKg: number) => void;
}
export const PRToast = forwardRef<PRToastHandle>((_, ref) => { ... });
```

From src/components/ProteinChart.tsx:
```typescript
interface ProteinChartProps { goal: number; refreshKey?: number; }
// Uses useFocusEffect to fetch data -- calls getDailyProteinTotals(startDate, endDate)
// Shows "No data yet" when data is empty
// Shows 4 filter pills: 1W, 1M, 3M, All
```

Test patterns:
- For Modals: render with visible={true}, use fireEvent.changeText for TextInput, fireEvent.press for buttons
- For DB mocks: jest.mock('../db', () => ({...})) at top of file
- For async state: use waitFor() from @testing-library/react-native
- For refs: const ref = React.createRef<PRToastHandle>(); render(<PRToast ref={ref} />); act(() => ref.current!.showPR(...));
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests for RenameModal, EditTargetsModal, and GoalSetupForm</name>
  <files>
    src/components/__tests__/RenameModal.test.tsx
    src/components/__tests__/EditTargetsModal.test.tsx
    src/components/__tests__/GoalSetupForm.test.tsx
  </files>
  <read_first>
    src/components/RenameModal.tsx
    src/components/EditTargetsModal.tsx
    src/components/GoalSetupForm.tsx
    src/types/index.ts
  </read_first>
  <action>
Create 3 test files in src/components/__tests__/.

**RenameModal.test.tsx** -- 5 tests:
Import: `import { render, fireEvent } from '@testing-library/react-native'` and `import { RenameModal } from '../RenameModal'`.

1. `renders title and current name`: render `<RenameModal visible={true} title="Rename Program" currentName="PPL" onClose={jest.fn()} onSave={jest.fn()} />`, assert `getByText('Rename Program')` exists, assert the TextInput has value 'PPL' via `getByDisplayValue('PPL')`
2. `Save button is disabled when name unchanged`: render with currentName="PPL", the Save button should be disabled (the component sets isDisabled when trimmed name equals currentName). Press Save, assert onSave NOT called.
3. `Save button is disabled when name is empty`: render with currentName="PPL", clear the input with `fireEvent.changeText(getByDisplayValue('PPL'), '')`, press Save, assert onSave NOT called.
4. `calls onSave with trimmed name when valid`: render with currentName="PPL", `fireEvent.changeText(getByDisplayValue('PPL'), '  Upper Body  ')`, press Save button via `fireEvent.press(getByText('Save'))`, assert `onSave` called with `'Upper Body'` (trimmed), and `onClose` also called.
5. `calls onClose when Cancel pressed`: render, `fireEvent.press(getByText('Cancel'))`, assert `onClose` called once.

**EditTargetsModal.test.tsx** -- 5 tests:
Import EditTargetsModal. Create a test dayExercise object: `{ id: 1, programDayId: 1, exerciseId: 1, targetSets: 3, targetReps: 10, targetWeightKg: 135, sortOrder: 0, supersetGroupId: null }`.

1. `renders exercise name and pre-fills targets`: render `<EditTargetsModal visible={true} onClose={jest.fn()} dayExercise={dayExercise} exerciseName="Bench Press" onSave={jest.fn()} />`, assert `getByText('Bench Press')` exists, assert `getByDisplayValue('3')` (sets), `getByDisplayValue('10')` (reps), `getByDisplayValue('135')` (weight) all exist.
2. `shows error for invalid sets`: change sets input to '0', press Save, assert `getByText('Sets must be a positive number')` exists.
3. `shows error for invalid reps`: change reps input to 'abc', press Save, assert `getByText('Reps must be a positive number')` exists.
4. `shows error for negative weight`: change weight input to '-5', press Save, assert `getByText('Weight must be zero or more')` exists.
5. `calls onSave with parsed values`: change sets to '4', reps to '8', weight to '185', press Save, assert `onSave` called with `(1, 4, 8, 185)` (dayExercise.id, setsNum, repsNum, weightNum).

For EditTargetsModal tests, use `getAllByDisplayValue` if multiple fields share similar values, or target inputs by their surrounding label text. Use `getByDisplayValue('3')` for sets, `getByDisplayValue('10')` for reps, `getByDisplayValue('135')` for weight since these are distinct values.

**GoalSetupForm.test.tsx** -- 4 tests:
At the top of the file:
```typescript
jest.mock('../../db', () => ({
  setProteinGoal: jest.fn().mockResolvedValue({ id: 1, dailyGoalGrams: 200, createdAt: '', updatedAt: '' }),
}));
import { setProteinGoal } from '../../db';
```

1. `renders title and input`: render `<GoalSetupForm onGoalSet={jest.fn()} />`, assert `getByText('Set your daily goal')` exists, assert placeholder '200' is present via `getByPlaceholderText('200')`.
2. `shows error for invalid input (0)`: type '0' into the input via `fireEvent.changeText(getByPlaceholderText('200'), '0')`, press 'Set Goal', assert `getByText('Please enter a number greater than 0')` exists.
3. `shows error for non-numeric input`: type 'abc', press 'Set Goal', assert error text shown.
4. `calls setProteinGoal and onGoalSet on valid submit`: type '150', press 'Set Goal'. Use `await waitFor(() => expect(setProteinGoal).toHaveBeenCalledWith(150))`. Also assert `onGoalSet` called with `150`.

Wrap test body in async and use `waitFor` for the async submit.
  </action>
  <verify>
    <automated>cd C:/Users/eolson/WorkPC-Development && npx jest src/components/__tests__/RenameModal.test.tsx src/components/__tests__/EditTargetsModal.test.tsx src/components/__tests__/GoalSetupForm.test.tsx --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - RenameModal.test.tsx contains `getByDisplayValue('PPL')` and tests for disabled Save when unchanged and empty
    - EditTargetsModal.test.tsx contains `getByText('Sets must be a positive number')` and `getByText('Reps must be a positive number')` and `getByText('Weight must be zero or more')`
    - GoalSetupForm.test.tsx contains `jest.mock('../../db'` and `setProteinGoal` and `getByText('Please enter a number greater than 0')`
    - All 3 test files pass with exit code 0
  </acceptance_criteria>
  <done>RenameModal (5 tests), EditTargetsModal (5 tests), GoalSetupForm (4 tests) all pass -- validation, empty state, and submit logic verified</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for ProteinProgressBar, PRToast, and ProteinChart</name>
  <files>
    src/components/__tests__/ProteinProgressBar.test.tsx
    src/components/__tests__/PRToast.test.tsx
    src/components/__tests__/ProteinChart.test.tsx
  </files>
  <read_first>
    src/components/ProteinProgressBar.tsx
    src/components/PRToast.tsx
    src/components/ProteinChart.tsx
    src/types/index.ts
    __mocks__/react-native-safe-area-context.js
    __mocks__/react-native-chart-kit.js
  </read_first>
  <action>
Create 3 test files in src/components/__tests__/.

**ProteinProgressBar.test.tsx** -- 5 tests:
At the top:
```typescript
jest.mock('../../db', () => ({
  setProteinGoal: jest.fn().mockResolvedValue({ id: 1, dailyGoalGrams: 200, createdAt: '', updatedAt: '' }),
}));
import { setProteinGoal } from '../../db';
```

1. `displays percentage and goal text`: render `<ProteinProgressBar goal={200} current={100} average={null} onGoalChanged={jest.fn()} />`, assert `getByText('50%')` exists (100/200 = 50%), assert `getByText(/100g consumed \/ 200g goal/)` exists.
2. `displays 7-day average when provided`: render with `average={175}`, assert `getByText(/7-day avg: 175g/)` exists.
3. `caps percentage at 100%`: render with `goal={100} current={150}`, assert `getByText('100%')` exists (capped at 1.0).
4. `enters edit mode on press`: press the progress bar container (use `fireEvent.press(getByText(/consumed/))`), assert `getByText('Daily protein goal (grams)')` appears (edit mode label), assert `getByDisplayValue('200')` shows pre-filled goal.
5. `saves new goal in edit mode`: enter edit mode by pressing, change value to '250' via `fireEvent.changeText(getByDisplayValue('200'), '250')`, press 'Save'. Use `await waitFor(() => expect(setProteinGoal).toHaveBeenCalledWith(250))` and assert `onGoalChanged` called with `250`.

**PRToast.test.tsx** -- 3 tests:
Import: `import React, { createRef } from 'react'`, `import { render, act } from '@testing-library/react-native'`, `import { PRToast, PRToastHandle } from '../PRToast'`.

Note: PRToast uses useSafeAreaInsets (mocked), Animated.timing (works in test env). The component renders null when no toast is active.

1. `renders nothing when no PR shown`: `const ref = createRef<PRToastHandle>(); const { toJSON } = render(<PRToast ref={ref} />);` assert `toJSON()` is null.
2. `shows exercise name and PR detail after showPR`: `const ref = createRef<PRToastHandle>(); const { getByText } = render(<PRToast ref={ref} />);` then `act(() => { ref.current!.showPR('Bench Press', 5, 225); });` assert `getByText('Bench Press')` exists, assert `getByText(/New 5-rep PR! 225 lbs/)` exists.
3. `exposes showPR via ref`: `const ref = createRef<PRToastHandle>();` render, assert `ref.current` is not null, assert `typeof ref.current!.showPR` is `'function'`.

**ProteinChart.test.tsx** -- 4 tests:
At the top:
```typescript
jest.mock('../../db', () => ({
  getDailyProteinTotals: jest.fn().mockResolvedValue([]),
}));
```
Also need NavigationContainer for useFocusEffect. Import `import { NavigationContainer } from '@react-navigation/native'` and wrap renders.

1. `shows empty state when no data`: render `<NavigationContainer><ProteinChart goal={200} /></NavigationContainer>`, use `await waitFor(() => expect(getByText('No data yet')).toBeTruthy())`.
2. `renders section header`: assert `getByText('PROTEIN INTAKE HISTORY')` exists.
3. `renders all 4 time range filter pills`: assert `getByText('1W')`, `getByText('1M')`, `getByText('3M')`, `getByText('All')` all exist.
4. `changes selected range on pill press`: press '1M' pill via `fireEvent.press(getByText('1M'))`, then `await waitFor(() => expect(getDailyProteinTotals).toHaveBeenCalledTimes(2))` (initial 1W + after pressing 1M). Verify getDailyProteinTotals was called with a start date roughly 1 month ago.

Wrap all ProteinChart test bodies in async functions and use waitFor for the async data loading.
  </action>
  <verify>
    <automated>cd C:/Users/eolson/WorkPC-Development && npx jest src/components/__tests__/ProteinProgressBar.test.tsx src/components/__tests__/PRToast.test.tsx src/components/__tests__/ProteinChart.test.tsx --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ProteinProgressBar.test.tsx contains `jest.mock('../../db'` and `getByText('50%')` and `getByText(/consumed/)` and tests edit mode
    - PRToast.test.tsx contains `createRef<PRToastHandle>()` and `ref.current!.showPR('Bench Press', 5, 225)` and `getByText('Bench Press')`
    - ProteinChart.test.tsx contains `jest.mock('../../db'` and `getDailyProteinTotals` and `getByText('No data yet')` and `getByText('1W')` and `getByText('All')`
    - All 3 test files pass with exit code 0
  </acceptance_criteria>
  <done>ProteinProgressBar (5 tests), PRToast (3 tests), ProteinChart (4 tests) all pass -- validation, ref API, empty state, and DB mock integration verified</done>
</task>

</tasks>

<verification>
Run the full complex component test suite:
```bash
npx jest src/components/__tests__/RenameModal.test.tsx src/components/__tests__/EditTargetsModal.test.tsx src/components/__tests__/GoalSetupForm.test.tsx src/components/__tests__/ProteinProgressBar.test.tsx src/components/__tests__/PRToast.test.tsx src/components/__tests__/ProteinChart.test.tsx --no-coverage
```
All 6 test files should pass. Expected: ~26 tests across 6 suites.
</verification>

<success_criteria>
- All 6 complex component test files exist in src/components/__tests__/
- RenameModal and EditTargetsModal have validation tests (empty input, invalid values)
- GoalSetupForm and ProteinProgressBar properly mock setProteinGoal from ../db
- PRToast test uses createRef to exercise the showPR imperative API
- ProteinChart test wraps in NavigationContainer and mocks getDailyProteinTotals
- All tests pass when run together
</success_criteria>

<output>
After completion, create `.planning/phases/18-component-and-context-tests/18-02-SUMMARY.md`
</output>
